# Historical executed notebooks

The original B0-only 04/05 notebooks are retained byte-for-byte, with saved outputs,
for provenance. They are not part of the active Run All workflow.

The original 04 has evaluation enabled and a hardcoded VM path. Do not execute it
as a review notebook. Use `../08_final_internal_test_evaluation.ipynb` and
`../09_final_35_cell_comparison.ipynb` for the safe final workflow.
