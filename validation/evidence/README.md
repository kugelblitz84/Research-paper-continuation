# Notebook 03 execution evidence

The original executed notebook was copied byte-for-byte before cleanup.
SHA-256: `98a2b34e118e328f40aee70df04b6dd8683da33dd485a817f2d682151d7fdad8`.

`03_shared_hierarchical_training.original_outputs.html` was exported from its saved outputs without executing any cells. It is a newly generated evidence export, not the earlier HTML mentioned by the researcher, which was not present in this checkout.
No experiment artifacts were reconstructed from notebook outputs.

The cleaned repository notebook retains the original scientific outputs, explicitly labeled as historical Azure execution evidence. Local fresh-kernel verification is stored separately in `validation/executed/03_shared_hierarchical_training.review_mode.ipynb` and `validation/03_shared_hierarchical_training.review_mode.html`. The clean HTML retains the original outputs and is a presentation export, not a new completed training execution.
